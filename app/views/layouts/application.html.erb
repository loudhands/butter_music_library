<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <title>Butter Music Library</title>
    <%= stylesheet( 'application', 'uploadify') %>
    <%= javascript( 'jquery-1.4.2', 'application') %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <div class="nav">
        <% if signed_in? %>
          Logged in as <%= current_user.email %> | <%= link_to "Log Out", sign_out_path %>
        <% end %>
      </div>
      
      <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <%- end -%>
      
      <%- if show_title? -%>
        <h1><%=h yield(:title) %></h1>
      <%- end -%>
      
      <%= yield %>
    </div>
  </body>
</html>
